package com.example.multiai;

import com.example.multiai.agent.Agent;
import com.example.multiai.manager.AgentManager;
import com.example.multiai.memory.CosmosMemoryStore;
//import org.junit.jupiter.api.Test;

import java.util.List;
import java.util.UUID;

//import static org.junit.jupiter.api.Assertions.*;

class AgentManagerTest {

/*    @Test
    void testAgentRegistration() {
        CosmosMemoryStore memoryStore = new CosmosMemoryStore(null); // Mock or null for basic test
        AgentManager manager = new AgentManager(UUID.randomUUID().toString(), memoryStore);

        Agent agent = new Agent("test-agent", "You are a test agent.", List.of(), List.of());
        manager.registerAgent(agent);

        assertDoesNotThrow(() -> manager.handleUserInput("Hello"));
    }*/
}